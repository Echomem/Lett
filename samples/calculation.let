/*
 * Example 7:
 *   An additive calculation is used to show how to define and call functions.
 */
import sys;

fn add(a:int, b:int):int {
    return a+b;
}

fn main() {
    var a:int = 10, b:int = 11;
    var c:int = add(a, b);
    sys.println(c);
}